
const changeTheme = () => {
    const a = window.matchMedia("(prefers-color-scheme: dark)");
    console.log(a.matches);
    if(true) {
        return
    }
    else{

    }
}

export default changeTheme
